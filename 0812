下面给出的是一套“符合当前SA5研究方向、可直接用于构造提案”的架构答案，而不是声称已经冻结的规范结论。

截至目前，TR 32.801-01仍处于研究阶段。当前草案已经提出Knowledge MnS、Knowledge MnS Producer/Consumer、知识表示、交换、共享、复用、生命周期和Ontology等关键问题，但具体架构实体、信息模型、MnS操作和解决方案仍有大量FFS/TBD。因此，你现在需要做的是形成一套自洽的架构主张，然后把它拆成提案。[TR 32.801-01 V0.3.0草案](https://www.3gpp.org/ftp/Specs/archive/32_series/32.801-01/32801-01-030.zip)；[FS_6G_OAM工作项](https://portal.3gpp.org/desktopmodules/WorkItem/WorkItemDetails.aspx?workitemId=1100014)

## 总体定义

TR 32.801-01当前把Knowledge定义为：

> structured and semantically related data

从SA5架构角度，建议进一步理解为：

> 知识是附带明确语义、关系、适用范围、来源、可信度和有效期，能够被不同MnF重复消费的机器可解释管理信息。

可以用下面这个关系理解：

```text
数据：
CPU利用率=95%，UPF告警A发生

分析结果：
UPF过载概率=0.87

知识：
UPF-A运行在Workload-7上；
Workload-7位于Zone-3；
Zone-3资源拥塞会导致UPF时延上升；
该结论适用于软件版本V5.2，置信度0.87，有效至T2。

决策：
将UPF-A迁移到Zone-4

动作：
Cloud Management Function执行迁移
```

所以，知识不是“所有数据的另一个名字”，也不是执行动作的功能。知识负责让数据和结论具有可理解、可关联、可复用的含义。

---

## 1. 为什么需要知识？

核心原因是：现有SA5管理系统有大量数据和分析能力，但这些数据之间的语义、关系、适用条件和经验通常隐藏在厂商私有逻辑中。

### 1.1 解决语义断裂

现有管理数据来自不同体系：

- NRM配置和拓扑
- PM、KPI、FM、Trace
- MDAS分析结果
- Intent和策略
- CCL控制结果
- NF Deployment和Cloud资源
- AI/ML模型输出

这些数据在语法上可能已经标准化，但系统未必知道：

- 两个对象是什么关系；
- 一个KPI由哪些数据计算；
- 一个告警可能影响哪些网络服务；
- 某个分析结论适用于什么网络范围；
- 一个结论是否已经过期；
- 结论是由哪些数据和算法得出的。

知识管理要把这些隐含关系显式化。

### 1.2 让经验能够跨MnF复用

现在一个MDAS发现的规律，可能只能在该MDAS内部使用；另一个Intent Handler、CCL或者Agent不能理解和复用它。

知识管理希望实现：

```text
一个MnF产生经验
        ↓
形成机器可解释知识
        ↓
其他MnF发现、获取并复用
```

### 1.3 支持跨域和跨层管理

6G管理问题通常跨越：

```text
Network Service
   ↓
Network Function
   ↓
NF Deployment / Workload
   ↓
Cloud Resource / Availability Zone
```

没有知识关系，系统看到的是四组孤立的数据；建立知识关系后，才能进行跨层根因分析、影响分析、资源优化和韧性管理。

### 1.4 提高自动化的可解释性和可信度

Intent、CCL和Agent不仅要输出“做什么”，还需要能够回答：

- 为什么这样做？
- 使用了哪些证据？
- 结论是否仍然有效？
- 适用于哪个网络范围？
- 与其他知识是否冲突？

因此，provenance、confidence、validity和scope不是附加项，而应当成为知识模型的核心。

### 1.5 SA5真正需要标准化的边界

不是所有厂商内部知识都要标准化。只有当知识需要跨越以下边界时，SA5标准化才产生价值：

- MnF之间；
- 厂商之间；
- 管理域之间；
- Network与Cloud之间；
- 不同自动化闭环之间。

---

## 2. 哪些实体创建、提供和消费知识？

这里首先要区分三个角色：

```mermaid
flowchart LR
    A["管理数据源"] --> B["知识创建者"]
    B --> C["Knowledge MnS Producer"]
    C --> D["Knowledge MnS Consumer"]
    D --> E["决策或管理动作"]
    E --> A
```

### 2.1 管理数据源

提供形成知识所需的原始证据，例如：

- NRM/Provisioning MnS；
- PM、FM、Trace；
- MDAS分析结果；
- Intent执行结果；
- CCL状态和控制结果；
- Cloud资源、部署、日志、指标和Trace；
- NDT和AI/ML模型输出。

数据源通常不等于知识创建者。

### 2.2 知识创建者

知识创建者是逻辑角色，目前未必需要定义成一个独立的标准化MnF。

可能的知识创建者包括：

- MDAS；
- NDT；
- AI/ML推理功能；
- Agent；
- Intent Handler；
- CCL中的分析功能；
- Cloud Management Function；
- 人工运维经验导入功能。

它负责把数据或者已有知识转换为新的知识，例如发现关系、生成规则、补充语义或者形成因果结论。

### 2.3 Knowledge MnS Producer

这是当前TR 32.801-01已经采用的角色名称。

其职责是：

- 暴露知识；
- 提供知识发现和查询能力；
- 控制知识访问；
- 发布知识更新；
- 提供知识内容和元数据。

关键架构原则是：

> Knowledge MnS Producer不一定是Knowledge Creator。

例如，MDAS创建知识，而一个共享知识库作为Knowledge MnS Producer对外提供该知识。

### 2.4 Knowledge MnS Consumer

可能包括：

- MDAS；
- Intent Handler；
- CCL；
- Agent；
- NDT；
- Fault/Performance Management MnF；
- Energy Saving MnF；
- Cloud Management Function；
- AI/ML训练和推理功能。

同一个MnF可以同时是Producer和Consumer。例如MDAS消费Cloud拓扑知识，生成根因知识，然后再通过Knowledge MnS提供给其他MnF。

### 2.5 是否需要独立的Knowledge Management Function？

这是一个很好的SA5提案点。

建议不要直接假定必须有一个集中式KMF，而是定义逻辑角色：

- Knowledge Creator；
- Knowledge MnS Producer；
- Knowledge MnS Consumer；
- Knowledge Repository/Catalog；
- Knowledge Governance功能。

这些角色既可以部署在一个MnF内，也可以分布在多个MnF中。也就是说：

> 统一服务契约，但不强制统一部署。

---

## 3. 知识管理需要哪些能力？

建议分成六组能力。

### A. 表示和语义能力

- 表示实体、属性和关系；
- 引用统一Ontology；
- 表示规则、约束、依赖和因果关系；
- 表示知识的范围、上下文和适用条件；
- 关联现有NRM对象、KPI、告警、Intent和Cloud资源。

### B. 知识创建能力

- 从管理数据创建知识；
- 从分析结果创建知识；
- 从已有知识推导新知识；
- 人工导入知识；
- 合并、映射和转换不同来源的知识；
- 验证知识是否满足相应Ontology。

SA5不一定标准化内部推理算法，但可以标准化创建结果、输入输出和可观察行为。

### C. 发现和访问能力

- 注册知识；
- 发现可用知识；
- 按类型、对象、范围、时间和关系查询；
- 获取完整知识或者知识子集；
- 对知识更新进行订阅和通知；
- 支持按需、周期和事件触发交付。

### D. 生命周期管理能力

至少要支持：

```text
Created → Validated → Active → Updated/Superseded
                               ↓
                         Deprecated → Retired
```

此外还需要：

- 版本管理；
- 有效期和失效判断；
- 知识依赖追踪；
- 来源数据变化后的重新验证；
- 知识撤回；
- 知识归档和删除；
- 知识与源数据生命周期解耦。

最后一点已经是当前草案的重要方向：源数据删除后，知识不一定立即删除；但必须保留来源记录，并重新评估其有效性。

### E. 共享、复用和联邦能力

- 多个Producer共享同一Knowledge Instance；
- 将一个域的知识复用于相似场景；
- 跨域知识查询；
- Ontology映射；
- 冲突检测和冲突解决；
- 避免要求所有知识集中存储。

推荐采用“联邦式知识管理”，而不是设计一个全网唯一的中央知识库。

### F. 治理和可信能力

- 身份认证和授权；
- 租户、域和用途控制；
- 来源追踪；
- 完整性保护；
- 机密性；
- 可信度和质量评价；
- 审计；
- 对错误知识的反馈和纠正；
- 知识冲突的优先级和权威来源判断。

---

## 4. 知识以什么模型表示？

建议采用“三层模型”。

### 第一层：Ontology

Ontology定义共同词汇和关系类型，例如：

```text
UPF isA NetworkFunction
Workload hosts NetworkFunction
AvailabilityZone contains Workload
Alarm affects ManagedEntity
Intent targets ManagementDomain
```

Ontology是知识的“语法和词典”，不是具体知识实例。

推荐采用：

- 3GPP公共核心Ontology；
- Domain-specific扩展；
- 厂商扩展；
- 不同Ontology之间的映射。

RDF/OWL可以作为一种Solution Set，但不应过早规定所有实现必须使用某一种图数据库或序列化技术。

### 第二层：Knowledge Descriptor

Knowledge Descriptor让知识可以被管理和发现，建议至少包含：

| 属性 | 含义 |
|---|---|
| knowledgeId | 全局或域内唯一标识 |
| knowledgeType | 事实、关系、规则、因果、经验等 |
| subject/scope | 涉及哪些MOI、域、切片、服务或Cloud资源 |
| ontologyRef | 使用的Ontology及版本 |
| creator/producer | 谁创建、谁提供 |
| sourceReferences | 来源数据或已有知识 |
| provenance | 产生过程、算法或证据链 |
| version | 知识版本 |
| validTime | 有效时间 |
| applicability | 适用条件 |
| confidence/quality | 置信度和质量 |
| lifecycleState | Active、Deprecated等 |
| accessPolicy | 谁可以访问 |
| payloadReference | 知识内容或获取位置 |

### 第三层：Knowledge Content

Knowledge Content保存具体事实、关系、规则或因果模型，例如：

```text
UPF-A hostedOn Workload-7
Workload-7 locatedIn Zone-3
Zone-3 hasCondition ResourceCongestion
ResourceCongestion causes UPFLatencyIncrease
```

再配上：

```text
confidence = 0.87
validFor = SoftwareVersion-5.2
validTime = [T1, T2]
evidence = {Alarm-23, KPI-47, Trace-9}
```

因此可以把一个可交换的知识单元定义成：

```text
Knowledge Package
    = Knowledge Descriptor
    + Knowledge Content
    + Ontology Reference
```

这比简单定义一个“有很多属性的类”更准确，因为核心价值不仅是属性，还有类型化关系、语义、证据、范围和生命周期。

---

## 5. 知识如何通过MnS交换？

建议Knowledge MnS同时支持资源式操作和知识特有操作。

### 5.1 基本交互过程

```mermaid
sequenceDiagram
    participant C as "Knowledge MnS Consumer"
    participant P as "Knowledge MnS Producer"
    participant R as "Knowledge Repository / Creator"

    C->>P: DiscoverKnowledge(selector)
    P-->>C: Knowledge descriptors
    C->>P: Query/GetKnowledge(scope, time, ontology, quality)
    P->>R: Resolve / retrieve / derive
    R-->>P: Knowledge package
    P-->>C: Descriptor + content/reference
    C->>P: SubscribeKnowledgeChanges()
    P-->>C: Notify(updated/deprecated/withdrawn)
```

### 5.2 Consumer请求中需要表达什么？

不能简单请求“给我所有知识”，请求应包含Knowledge Selector，例如：

- 目标对象或者管理范围；
- 所需知识类型；
- 所需关系；
- 时间范围；
- 最低新鲜度；
- 最低置信度；
- Ontology或表示格式；
- 使用目的；
- 完整结果还是增量结果。

### 5.3 可能需要的操作

这些目前可以作为提案中的候选操作，而不是宣称已经标准化：

- register/publishKnowledge；
- discoverKnowledge；
- getKnowledge；
- queryKnowledge；
- updateKnowledge；
- withdrawKnowledge；
- subscribeKnowledgeChange；
- notifyKnowledgeChange；
- validateKnowledge；
- provideKnowledgeFeedback。

其中，创建、读取、更新、删除和订阅通知应尽量复用SA5已有的通用MnS操作模式；只有语义查询、推理请求、知识验证等无法用通用操作表达的能力，才考虑定义Knowledge-specific操作。

### 5.4 交付模式

Knowledge Provision应支持：

- On-demand；
- Periodic；
- Event-triggered；
- Subscription/notification；
- 完整知识包；
- 增量或差异更新；
- 大对象的异步文件、流或者引用式交付。

还需要保持三个层次相互独立：

```text
MnS操作语义
    ≠ 知识信息模型
    ≠ RDF/JSON-LD/文件等传输编码
```

---

## 6. 与Data、MDAS、Intent、CCL、Agent和Cloud是什么关系？

| 对象 | 主要职责 | 与Knowledge的关系 |
|---|---|---|
| Data/DMFW | 采集、传输、存储管理数据 | 数据是知识的证据；知识为数据增加语义和关系 |
| MDAS | 对数据进行分析，生成洞察和预测 | 创建和消费知识，但不等于Knowledge Management |
| Intent | 表达期望目标和约束 | Knowledge帮助Intent Handler理解环境、影响和可行方案 |
| CCL | 感知、分析、决策、执行闭环控制 | Knowledge为闭环提供共享上下文和经验，但不负责执行闭环 |
| Agent | 自主理解、规划、交互和调用能力 | Knowledge是Agent可治理的共享上下文或长期记忆 |
| Cloud | 管理计算、存储、网络、Workload和NF Deployment | 提供基础设施知识，并消费知识进行韧性和资源管理 |

### 6.1 与Data和DMFW

二者不能重复建设。

```text
DMFW关注：
数据在哪里、如何采集、如何传输、如何访问。

Knowledge Management关注：
数据是什么意思、相互有什么关系、在哪些条件下可以复用。
```

知识也可以反向指导数据采集，例如根据根因知识决定需要采集哪些KPI或Trace。

### 6.2 与MDAS

MDAS和Knowledge最容易混淆。

```text
MDAS：
输入数据 → 执行分析 → 输出分析结果

Knowledge Management：
输入数据/分析结果/已有知识
→ 增加语义、来源、范围和有效性
→ 对不同MnF暴露并管理
```

并非每个MDAS输出都自动是知识。只有经过语义化、可寻址、可复用和生命周期管理后，它才成为可共享Knowledge Instance。

### 6.3 与Intent

Intent表示“希望达到什么结果”，不是关于世界状态的知识。

例如：

```text
Intent：
将网络能耗降低10%，同时保证SLA。

Knowledge：
小区A与小区B存在覆盖重叠；
关闭小区A会使区域X容量下降20%；
该规律在工作日夜间有效。
```

Intent Handler利用知识进行意图分解、可行性判断、冲突检测和方案选择；Intent执行结果又可以反过来形成经验知识。

### 6.4 与CCL

CCL负责让闭环运行，Knowledge负责让不同闭环共享认知。

知识可以支撑CCL中的：

- Awareness；
- Analysis；
- Decision。

CCL产生的动作、结果和失败经验又可以更新知识。Knowledge MnS不应直接取代闭环控制或者配置执行MnS。

### 6.5 与Agent

Agent是一种可能的知识消费者和创建者，不应直接等同于Knowledge Management Function。

Agent可以：

- 查询网络知识；
- 根据知识形成计划；
- 把观察、执行结果和经验形成新知识；
- 与其他Agent共享知识。

但SA5应主要标准化Agent与知识系统之间的接口、语义和治理，不宜标准化Agent内部Prompt、LLM记忆或者推理链。

### 6.6 与Cloud Management

Cloud是知识管理非常重要的跨域应用场景。

需要建立的关系包括：

```text
Network Service
  → composedOf Network Function
  → realizedBy NF Deployment
  → contains Workload
  → hostedOn Cloud Resource
  → locatedIn Failure Domain
```

这样，当Cloud Availability Zone发生异常时，系统才能推导：

- 哪些Workload受到影响；
- 哪些NF Deployment受到影响；
- 哪些NF和Network Service受到影响；
- 是否需要重新部署、迁移或者扩容。

Knowledge Management提供跨层关联和推理依据，Cloud Management Function仍然拥有并执行Cloud资源操作。

---

## 一个完整例子

假设Zone-3出现资源拥塞：

1. Cloud观测系统产生指标、日志和告警。
2. MDAS分析得出“Zone-3未来10分钟发生性能退化的概率为0.87”。
3. 知识创建者把它与UPF、Workload、服务和SLA关系组合成Knowledge Package。
4. Knowledge MnS Producer发布该知识。
5. Intent Handler发现当前节能Intent可能与SLA冲突。
6. CCL或者Agent选择将UPF-A迁移到Zone-4。
7. Cloud Management Function执行迁移。
8. 执行结果、效果和失败原因再次形成知识，用于下次复用。

这里：

```text
Cloud产生基础设施数据
MDAS产生分析结果
Knowledge Management形成可复用认知
Intent定义目标
Agent/CCL作出决策
Cloud Management执行动作
```

## 我认为SA5应坚持的七个架构原则

1. Knowledge Creator与Knowledge MnS Producer解耦。
2. 知识生命周期与源数据生命周期解耦。
3. 支持联邦式知识管理，不强制中央知识库。
4. 采用公共核心Ontology加领域扩展。
5. Knowledge Descriptor与Knowledge Content分离。
6. 来源、有效期、范围、质量和版本应是必选的核心元数据。
7. 优先复用通用MnS操作，只为知识特有行为定义新操作。

这六个问题实际上可以直接形成你后续提案的顶层骨架：先提架构原则，再分别落到Actor、能力、信息模型、MnS行为、Ontology和与现有体系边界。
