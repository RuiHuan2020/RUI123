# 2026-08-12 学习产出：用 TS 28.530 给 TS 28.533 的架构填入业务

> 学习主题：TS 28.530 × TS 28.533  
> 目标：把“管理什么、为什么管理”与“谁生产/消费哪些管理服务”连成一条完整主线。  
> 建议用时：约 2 小时。

## 1. 今日结论

TS 28.533 定义服务化管理架构语言，回答 **管理能力怎样被生产、消费、组合、发现和授权**；TS 28.530 定义 5G 网络与网络切片的概念、角色、生命周期和高层用例，回答 **为了什么业务目标，需要管理哪些对象和过程**。

两者的关系可以概括为：

~~~text
TS 28.530：业务角色 + 业务对象 + 生命周期 + 管理目标
                         ↓
TS 28.533：MnF + MnS producer/consumer + A/B/C 组件 + 交互范式
~~~

## 2. 28.530 与 28.533 映射图

~~~mermaid
flowchart TD
  CSC["CSC<br/>提出通信服务需求"] --> CSP["CSP<br/>设计并运营通信服务"]
  CSP -->|ServiceProfile / SLS| NOP["NOP / Network Slice Management MnF"]
  NOP -->|消费 RAN NSSI MnS| RAN["RAN Management MnF"]
  NOP -->|消费 CN NSSI MnS| CN["CN Management MnF"]
  NOP -->|消费传输管理能力| TN["TN Manager"]
  RAN -->|消费 NF 级 MnS| RANNF["gNB / RAN NF MnF"]
  CN -->|消费 NF 级 MnS| CNNF["AMF / SMF / UPF MnF"]
  NOP -->|生产 NSI 级 MnS| CSP
~~~

图中的组织角色和 SBMA 角色不是固定一一对应：producer/consumer 是针对某个具体 MnS 而言的。同一个 NOP 管理功能对 CSP 可以是 producer，对 RAN/CN 域管理功能则是 consumer。

## 3. 业务角色与 SBMA 角色映射表

| 28.530 角色/实体 | 主要业务职责 | 可能承担的 28.533 角色 | 典型 MnS |
|---|---|---|---|
| CSC | 使用通信服务，提出业务需求 | 通常通过 CSP 间接消费管理信息；NSaaS 场景可成为受限 consumer | 服务状态、SLA/KPI 查询 |
| CSP | 设计、构建、运营通信服务 | 消费网络/切片 MnS；也可生产面向 CSC 的服务管理能力 | NSI 供给、性能、故障、服务保障 |
| NOP | 设计、构建、运营网络和网络切片 | 对 CSP 是 producer；对 RAN/CN/TN 域是 consumer | NSI/NSSI provisioning、CM、PM、FM |
| RAN Management MnF | 管理 RAN 子网、NSSI 和 RAN NF | 对上层 NOP 是 producer；对 gNB/NF 是 consumer | RAN NSSI、gNB 配置/性能/故障 MnS |
| CN Management MnF | 管理 CN 子网、NSSI 和 CN NF | 对上层 NOP 是 producer；对 AMF/SMF/UPF 是 consumer | CN NSSI、5GC NF 配置/性能/故障 MnS |
| NEP/NF MnF | 提供具体网络功能及其管理能力 | 生产 NF 级 MnS | NRM CRUD、告警、测量和文件/流数据 |
| VISP/DCSP/NFV MANO | 提供虚拟化和基础设施能力 | 被 3GPP 管理系统消费其生命周期/资源接口 | NS/VNF LCM、资源 PM/FM/CM |

## 4. NetworkSlice 生命周期与 MnS 对照

~~~mermaid
flowchart LR
  A["Preparation<br/>准备"] --> B["Commissioning<br/>开通"]
  B --> C["Operation<br/>运行"]
  C -->|需求变化| C
  C --> D["Decommissioning<br/>退网"]
~~~

| 生命周期阶段 | 28.530 的业务活动 | 需要的 MnS 能力 | 主要交互范式 | 典型信息 |
|---|---|---|---|---|
| Preparation | 切片设计、容量规划、NF 评估/上架、环境准备、可行性分析 | 服务发现、能力/库存查询、可行性检查、资源预留 | Request-response；部分结果用 Subscribe-notify | ServiceProfile、候选 NSSI/NF、能力和容量 |
| Commissioning | 创建 NSI，创建或复用 NSSI，分配并配置资源 | NSI/NSSI provisioning、NRM CRUD、异步过程监控 | Request-response + Subscribe-notify | Controller/Job、NetworkSlice、NetworkSliceSubnet、SliceProfile |
| Operation | 激活、性能监督、故障监督、容量规划、修改、去激活 | CM、PM、FM、文件/流上报、阈值监控、分析与闭环 | 三种范式都会使用 | 状态、告警、测量/KPI、文件、阈值事件 |
| Decommissioning | 释放非共享组成，清理共享资源上的切片专属配置，终止 NSI | 去分配、删除/修改 MOI、状态与结果通知 | Request-response + Subscribe-notify | 共享关系、资源引用、最终状态和审计记录 |

## 5. MnS 组件在切片管理中的使用

| 管理能力 | Type A：操作/通知 | Type B：管理模型 | Type C：性能/故障信息 |
|---|---|---|---|
| NSI/NSSI 供给 | allocate/deallocate、CRUD、过程通知 | NetworkSlice、NetworkSliceSubnet、Controller、Feasibility Job | 通常不需要 |
| 配置管理 | create/get/modify/delete、配置变化通知 | 28.541 的切片、RAN、5GC NRM | 不需要 |
| 性能管理 | 测量任务控制、文件通知、流连接、阈值通知 | PerfMetricJob、Files/File、NSI/NSSI/NF 对象 | 测量类型和 KPI |
| 故障管理 | 订阅、告警通知、告警表查询 | 被监督的 NSI/NSSI/NF 资源对象 | AlarmRecord、原因、严重度、相关性 |

## 6. ServiceProfile → NSI → NSSI → NF 对象关系

~~~mermaid
flowchart TD
  REQ["通信服务需求 / SLS"] --> SP["ServiceProfile<br/>NSI 级要求"]
  SP --> NS["NetworkSlice<br/>NSI"]
  NS -->|networkSliceSubnetRef| TOP["Top NetworkSliceSubnet"]
  TOP -->|networkSliceSubnetRefList| RANNSSI["RAN NSSI"]
  TOP -->|networkSliceSubnetRefList| CNNSSI["CN NSSI"]
  RANNSSI -->|sliceProfileList| RANSP["RAN SliceProfile"]
  CNNSSI -->|sliceProfileList| CNSP["CN SliceProfile"]
  RANNSSI -->|managedFunctionRefList| GNB["GnbCuCp / GnbCuUp / GnbDu"]
  CNNSSI -->|managedFunctionRefList| CORE["AMF / SMF / UPF / ..."]
  GNB --> CELL["NrCellCu / NrCellDu"]
~~~

### 对象分工

| 对象 | 回答的问题 |
|---|---|
| ServiceProfile | 端到端 NSI 要满足什么服务要求？ |
| NetworkSlice | 哪个 NSI 承载这些要求，当前状态是什么？ |
| SliceProfile | 某个 NSSI 需要承担哪些分解后的要求？ |
| NetworkSliceSubnet | 该 NSSI 由哪些 NF、子 NSSI 和传输端点组成？ |
| ManagedFunction/NF | 具体由哪些 gNB、AMF、SMF、UPF 等实现？ |

ServiceProfile 不直接等同于底层配置。它需要经过可行性检查和需求分解，形成一个或多个 SliceProfile，再驱动 NSSI/NF 的选择、创建或修改。

## 7. 端到端案例：园区 URLLC 切片

### 业务输入

- CSC：智慧工厂。
- CSP/NOP：运营商。
- 需求：园区限定覆盖、低时延、高可靠、500 个 UE、严格租户隔离。
- 现状：已有可共享的 RAN NSSI；CN 没有满足隔离要求的 NSSI。

### 交互过程

| 步骤 | 业务动作 | Producer | Consumer | 交互范式 |
|---|---|---|---|---|
| 1 | 发现 NSI provisioning MnS，核对版本和能力 | Discovery/NS MnS producer | CSP/NOP 上层功能 | Request-response |
| 2 | 完成认证授权，取得 FactoryA scope 权限 | Authentication/Authorization service | NS MnS consumer | Request-response |
| 3 | 提交 ServiceProfile，执行可行性检查 | NS Management MnF | CSP/NOP consumer | Request-response |
| 4 | 创建作业/控制器并持续报告进度 | NS Management MnF | CSP/NOP consumer | Subscribe-notify 或查询 |
| 5 | 分解 RAN/CN SliceProfile | NS Management MnF | 内部编排逻辑 | 本地处理 |
| 6 | 复用 RAN NSSI、分配新 CN NSSI | RAN/CN Management MnF | NS Management MnF | Request-response + Subscribe-notify |
| 7 | 创建 NetworkSlice 并关联 NSSI | NS Management MnF | CSP/NOP consumer | Request-response |
| 8 | 激活后交付 NSI 级状态、PM、FM | NS Management MnF | CSP/运维 consumer | Subscribe-notify、Connect-streaming、文件 |
| 9 | KPI 越限后修改 CN NSSI 容量 | CN/NS Management MnF | 闭环或运维 consumer | 通知 + Request-response |
| 10 | 合同结束，去激活并清理专属配置 | NS/RAN/CN Management MnF | CSP/NOP consumer | Request-response + Subscribe-notify |

### 多租户与暴露治理

对 FactoryA 暴露的 MnS 应满足：

- 只能访问该租户的 NetworkSlice、ServiceProfile 和授权的性能/故障数据；
- 共享 RAN NSSI 的内部拓扑、其他租户引用和全局容量不可见；
- 允许查询状态和 KPI，但修改/删除能力按 SLA 单独授权；
- 通知、文件和流数据沿用相同 tenant scope，防止旁路泄漏。

## 8. 5 分钟汇报稿

今天我把 TS 28.530 和 TS 28.533 串联起来理解。28.530 描述的是 5G 管理的业务对象、角色和生命周期，28.533 描述的是这些管理能力怎样以 MnS 的形式被生产和消费。

在 28.533 中，MnS 是服务化管理架构的基本构件。它至少由 Type A 操作/通知和 Type B 信息模型组成；如果涉及性能或故障，还会加入 Type C 信息。MnF 是承担 MnS producer 或 consumer 角色的逻辑实体。同一个 MnF 可以消费下层服务，再聚合成上层服务。

映射到 28.530，CSC 提出通信服务需求，CSP 将其形成服务要求，NOP 负责创建或复用满足要求的网络切片。NOP 的网络切片管理功能对 CSP 是 MnS producer，对 RAN、CN 和 TN 域管理功能则是 consumer。

NetworkSlice 生命周期分为准备、开通、运行和退网。准备阶段主要进行能力发现、规划和可行性检查；开通阶段创建或复用 NSSI，并建立 NetworkSlice；运行阶段使用配置、性能、故障、文件和流式 MnS；退网阶段释放非共享资源，并清理共享资源上的切片专属配置。

对象关系上，ServiceProfile 描述 NSI 级业务要求，要求经过分解形成 SliceProfile，驱动 RAN/CN NSSI 的选择或创建。NetworkSlice 关联 NetworkSliceSubnet，NSSI 再关联 gNB、AMF、SMF、UPF 等具体网络功能。

以智慧工厂 URLLC 为例，顶层切片管理 MnF 消费 RAN 和 CN 域 MnS，复用已有 RAN NSSI、创建新的 CN NSSI，再向上生产一个受租户权限限制的 NSI 管理服务。控制操作采用 request-response，状态和告警使用 subscribe-notify，实时性能数据可使用 connect-streaming。这就是 28.530 的业务与 28.533 的架构之间的映射。

## 9. 自测题与参考要点

1. **为什么 CSP/NOP 不能固定标为 producer 或 consumer？**  
   因为 producer/consumer 是相对于某个具体 MnS 的角色；同一实体可以对上生产、对下消费。

2. **ServiceProfile 和 SliceProfile 的区别是什么？**  
   前者描述 NSI 级服务要求，后者描述分解到 NSSI 的要求。

3. **开通阶段为什么需要 request-response 和 subscribe-notify？**  
   创建请求可以同步受理，但资源分配和配置可能耗时，需要异步状态/结果通知或轮询。

4. **Type B 和 Type C 的边界是什么？**  
   Type B 表达受管实体的信息模型；Type C 表达与实体相关的性能或故障信息语义。

5. **为什么退网时不能直接删除全部 NSSI？**  
   NSSI 或底层资源可能被多个 NSI 共享，只能释放非共享组成，并清理当前 NSI 的专属配置/引用。

6. **租户隔离只限制 CRUD 是否足够？**  
   不足。通知、性能文件、流数据、发现结果和对象引用也必须应用一致的 tenant scope。

## 10. 明日完成记录

- [ ] 闭卷写出 MnS、MnF、producer、consumer。
- [ ] 闭卷画出 Type A+B 和 Type A+B+C。
- [ ] 阅读 28.530：4.1、4.3、4.6、4.8、5.4.2、5.4.3。
- [ ] 不看参考答案，重新画第 2 节映射图。
- [ ] 不看参考答案，重新画第 6 节对象关系图。
- [ ] 用自己的语言补充一个不同于园区 URLLC 的案例。
- [ ] 完成第 9 节六道自测题。
- [ ] 在 5 分钟内完成第 8 节内容的口头讲解。

### 我的补充案例

> 在此填写：例如大规模物联网、车联网、远程医疗或固定无线接入切片。

### 今天仍不清楚的问题

1. 
2. 
3. 

### 阅读后需要修正的内容

1. 
2. 
3. 

## 11. 完成标准

明日学习完成的判断标准不是“读完章节”，而是能够脱离规范完成以下三件事：

1. 从 CSC 需求一直讲到 NSI、NSSI 和 NF，并说明每一步的业务目的。
2. 为每次跨实体交互标明 MnS producer、consumer 和交互范式。
3. 说明 Type A/B/C、服务发现、授权和多租户治理如何共同形成一项可安全消费的管理能力。
